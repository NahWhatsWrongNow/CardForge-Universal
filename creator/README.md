# Creator Suite

Run from repository root:

```bash
python -m http.server 8080
```

Open `http://localhost:8080/creator/`.

Current modules:
- Plugin load report.
- Card JSON preview form for minions.
- Shared pack loading via manifest indexes.
